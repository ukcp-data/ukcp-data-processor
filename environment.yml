name: ukcp-dp
channels:
  - conda-forge
  - defaults
dependencies:
  - iris=2.4.0
  - python=3.7.6
  - pip:
    - git+https://github.com/ukcp-data/UKCP18_CVs.git
    - git+https://github.com/ukcp-data/ukcp-spatial-files.git
prefix: /usr/local/miniconda/envs/ukcp-dp
